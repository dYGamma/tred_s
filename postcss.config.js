// postcss.config.js (в корне проекта)

module.exports = {
  plugins: {
    tailwindcss: {},    // Подключаем Tailwind CSS
    autoprefixer: {},   // Подключаем Autoprefixer (добавляет вендорные префиксы)
  },
};
